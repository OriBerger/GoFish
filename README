HOW TO RUN THE APP

Frontend:

1. Enter directory frontend/my-app.
2. Run `npm install`.
3. Run `npm start`.
4. You can see result in `http://localhost:3000`.

Backend:

1. Enter directory bcakend.
2. Run `npm start`.
3. Run `npx ts-node src/index.ts`.
4. You can see resoult in `http://localhost:3001`.

left to do:

1. prevent SQL injection
2. add contact delete logic
3. add send evol message to cintacts
4. create eval message
5. statistics
6.
